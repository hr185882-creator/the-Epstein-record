# Repository Governance

Production changes should be proposed through pull requests, pass the repository quality checks, and preserve visible Git and Vercel deployment provenance.

Once branch rules are enabled, direct pushes to `main` should be blocked. Required checks should include the repository quality workflow and Vercel deployment status. Signed or GitHub-verified commits should be required for changes entering the protected branch.
